Цикл while
Используется, когда неизвестно точное количество итераций.

✅ Пример:

csharp
Копировать
Редактировать
int i = 0;
while (i < 5)
{
    Debug.Log("Число: " + i);
    i++; // Увеличиваем `i`, иначе будет бесконечный цикл!
}